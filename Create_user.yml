---
- name: Create user and group
  tasks: 
  - name: Create group
    win_group:
      name: ansible
      description: ansible admin
      state: present
  -name: Create user
    win_user:
      name: ansible
      password: ansible
      state: present
      groups:
        - ansible
  